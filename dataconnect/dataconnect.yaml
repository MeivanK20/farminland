specVersion: "v1"
serviceId: "farminland"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "farminland-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
